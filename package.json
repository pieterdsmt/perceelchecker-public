{
  "name": "perceelchecker",
  "version": "5.0.0",
  "private": true,
  "scripts": {
    "check": "node --check assets/js/app.js && node --check api/proxy.js",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "devDependencies": {
    "vercel": "latest"
  }
}